import XhButtonGroup from '../button/src/button-group.vue';

/* istanbul ignore next */
XhButtonGroup.install = function (Vue) {
  Vue.component(XhButtonGroup.name, XhButtonGroup);
};

export default XhButtonGroup;
